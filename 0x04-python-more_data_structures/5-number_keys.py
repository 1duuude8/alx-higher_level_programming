#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    counts number of keys in a dict

    args:
        a_dictionary (dict): a dictonarty containing elements
    returns:
        int: return number of keys in a dictionnary
    """
    return len(a_dictionary)
